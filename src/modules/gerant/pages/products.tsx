import { GerantContainer } from '../components/GerantContainer';

export const GerantProducts = () => {
  return (
    <GerantContainer clicked='produits'>
      <div>
        <h2>Gerant Mes Produits</h2>
      </div>
    </GerantContainer>
  );
};
